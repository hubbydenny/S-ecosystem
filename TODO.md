# TODO
## Done
- [x] toml++ config system (generate/read/state)
- [x] Single dictionary: template ↔ struct ↔ loadConfig
- [x] Logo colors from color.h

## In progress
- [x] `create_directories(parent_path())` in ensureConfig
- [ ] `--config` flag
- [ ] color system that interract with fetch

## Later
- [ ] Selectable logos
- [ ] Push to GitHub + README
