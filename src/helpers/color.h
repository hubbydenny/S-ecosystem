// color.h — ANSI escape-коды для цвета текста.
//
// Сюда пойдёт таблица цветов (например kRed, kGreen, kReset) и
// обёртки вроде paint(color, text). Важно: красить только при
// подключённом TTY (isatty(1)) и поддержке NO_COLOR/TERM.
//
// (код появится по запросу — сейчас только заготовка)
// 
